# Service to trigger weight detection baseline recalibration
# Returns immediately - calibration happens asynchronously
---
bool success
string message
int32 wait_time_ms  # Time to wait for calibration to complete (milliseconds)
